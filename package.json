{
	"name": "budgie-ts-object-molder",
	"version": "0.0.1",
	"description": "Budgerigar's object validator in TypeScript",
	"main": "lib/budgie-ts-object-molder.js",
	"types": "lib/budgie-ts-object-molder.d.ts",
	"scripts": {
		"build": "tsc",
		"prepublishOnly": "npm test",
		"prepare": "npm run build",
		"start": "npm run build && node lib/budgie-ts-object-molder.js",
		"test": "jest --verbose",
		"test-watch": "jest --verbose --watchAll"
	},
	"author": "",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/GitBudgie/budgie-ts-object-molder.git"
	},
	"homepage": "https://github.com/GitBudgie/budgie-ts-object-molder#readme",
	"bugs": {
		"url": "https://github.com/GitBudgie/budgie-ts-object-molder/issues"
	},
	"files": [
		"lib/**/*"
	],
	"devDependencies": {
		"@types/jest": "^26.0.15",
		"@types/node": "^14.14.5",
		"jest": "^26.6.1",
		"ts-jest": "^26.4.3",
		"typescript": "^4.0.5"
	}
}
